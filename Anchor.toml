[features]
seeds = false
skip-lint = false

[programs.devnet]
meme_fate_oracle = "YourProgramIdHere"  # Replace post-deploy

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn install && yarn test"
